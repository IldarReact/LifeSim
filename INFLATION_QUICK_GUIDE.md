# 📊 Инфляция в ArtSurv - Быстрая Шпаргалка

## 🎯 Главное за 30 секунд

**Инфляция = реалистичный рост цен в игре**

- ✅ Применяется **1 раз в год** (Q1)
- ✅ Цены **НИКОГДА не падают** (минимум +0.1%)
- ✅ **Кумулятивный эффект** - каждый год накапливается
- ✅ **Разные категории** растут с разной скоростью

---

## 🏗️ Как это работает (5 слоев)

```
1. JSON (economy.json)
   inflation: 2.5%
   inflationHistory: [2.5, 2.3, 2.7]
   
2. Loader (Zod validation)
   ↓
   
3. Core Logic (чистые функции)
   generateYearlyInflation() → новая инфляция
   getInflatedPrice() → применение к ценам
   
4. State (Zustand)
   processInflation() → обновление в Q1
   
5. UI (React)
   useInflatedPrice() → автоматическое применение
```

---

## 📈 Категорийные мультипликаторы

| Категория | Множитель | Скорость роста |
|-----------|-----------|----------------|
| 🏠 housing | **×1.5** | Самый быстрый |
| 🏢 realEstate | ×1.5 | Быстрый |
| 💼 business | ×1.3 | Выше среднего |
| 📚 education | ×1.2 | Выше среднего |
| 🏥 health | ×1.1 | Средний+ |
| 🚗 transport | ×1.0 | Средний |
| 💰 salaries | ×0.95 | Ниже среднего |
| 💇 services | ×0.9 | Медленный |
| 🍎 food | **×0.5** | Самый медленный |

---

## 💻 Использование в коде

### ✅ ПРАВИЛЬНО

```tsx
// В UI компонентах
import { useInflatedPrice } from '@/core/hooks'

function ShopItemCard({ item }: Props) {
  const price = useInflatedPrice(item)
  return <span>${price.toLocaleString()}</span>
}

// Для зарплат
function CandidateCard({ candidate }: Props) {
  const salary = useInflatedPrice({ salary: candidate.requestedSalary })
  return <span>${salary.toLocaleString()}/мес</span>
}

// Для списков
function JobsList({ jobs }: Props) {
  const jobsWithInflation = useInflatedPrices(jobs)
  return jobsWithInflation.map(job => (
    <div>${job.inflatedPrice}</div>
  ))
}
```

### ❌ НЕПРАВИЛЬНО

```tsx
// НЕ используй базовую цену напрямую
<span>${item.price}</span>

// НЕ применяй инфляцию вручную
const price = item.price * (1 + inflation / 100)
```

---

## 🔢 Примеры расчётов

### Жильё (×1.5)
```
Базовая цена: $100,000
Инфляция: 2.5% → 2.7% → 3.0%

Год 1: $100,000 × 1.0375 = $103,750
Год 2: $103,750 × 1.0405 = $107,956
Год 3: $107,956 × 1.045 = $112,818

Итого: +12.8% за 3 года
```

### Еда (×0.5)
```
Базовая цена: $5
Инфляция: 2.5% → 2.7% → 3.0%

Год 1: $5 × 1.0125 = $5.06
Год 2: $5.06 × 1.0135 = $5.13
Год 3: $5.13 × 1.015 = $5.21

Итого: +4.2% за 3 года
```

---

## 📁 Ключевые файлы

| Файл | Назначение |
|------|-----------|
| `core/lib/calculations/inflation-engine.ts` | Генерация инфляции |
| `core/lib/calculations/price-helpers.ts` | Применение к ценам |
| `core/model/logic/turns/inflation-processor.ts` | Обработка в Q1 |
| `core/hooks/useInflation.ts` | UI хуки |

---

## 🎮 Игровой эффект

### Краткосрочно (1-2 года)
- Почти незаметно (+2-3%)
- Зарплаты индексируются

### Среднесрочно (3-5 лет)
- Накопленный эффект (+10-15%)
- Недвижимость дорожает (+20-30%)

### Долгосрочно (10+ лет)
- Критичный эффект (+30-50%)
- Ранние инвестиции окупаются
- Зарплаты отстают

---

## 🔧 Для балансировки

**Изменить базовую инфляцию:**
```json
// shared/data/world/countries/us/economy.json
{
  "inflation": 2.5  // ← Измени здесь
}
```

**Изменить скорость роста категорий:**
```typescript
// core/lib/calculations/inflation-engine.ts
export const INFLATION_MULTIPLIERS = {
  housing: 1.5,  // ← Измени здесь
  food: 0.5
}
```

**Изменить волатильность:**
```typescript
// core/lib/calculations/inflation-engine.ts
export const INFLATION_SETTINGS = {
  minInflation: 0.1,
  maxInflation: 20,
  dampingFactor: 0.6,  // ← Тренд-следование
  volatility: 0.8      // ← Случайность
}
```

---

## 📚 Подробная документация

Полное описание: **[INFLATION_SYSTEM.md](./INFLATION_SYSTEM.md)**

---

**Версия**: 1.0  
**Дата**: 2024-12-05
