# Архитектура ArtSurv

## 1. 🗺️ Глобальная Карта Потоков Данных (Data Flow)

```mermaid
graph TD
    A[Static Data JSON] -->|Loaders| B(Game State / Store)
    B -->|Selectors| C[Feature Components (UI)]
    C -->|User Actions| D{Action Handlers}
    D -->|Mutations| B
    E[Game Loop / Turn] -->|Calculations| B
    B -->|Persistence| F[Local Storage / Save File]
    G[Business Data] -->|Player Employment| H[Jobs System]
```

## 2. 🏗️ Структура Проекта (High-Level)

```text
root/
├── app/            # Next.js Pages & Routing
├── core/           # Core Domain Logic (Headless)
│   ├── lib/        # Pure Calculation Engines (Financial, Business, Inflation)
│   ├── model/      # Game State, Slices & Turn Pipeline
│   ├── data-loaders/ # Zod Validators & Data Loading
│   └── types/      # Global TypeScript Definitions
├── features/       # UI & Feature-Sliced Logic (View Layer)
│   ├── activities/ # Game Activities (Bank, Work, Education)
│   ├── gameplay/   # Main Game Screen Layouts
│   ├── menu/       # Main Menu
│   └── setup/      # Character Creation & Game Setup
├── shared/         # Shared Static Data (JSONs)
│   └── data/       # Game Content (World, Items, Events)
└── docs/           # Documentation
```

---

## 3. 🗄️ Layer 1: Static Data (Фундамент)

_Неизменяемые конфигурационные файлы, определяющие баланс и контент игры._

**Расположение:** `shared/data/`

### 🌍 World & Economy (Мир и Экономика)

| Файл / Путь                     | Назначение             | Описание                                                    |
| :------------------------------ | :--------------------- | :---------------------------------------------------------- |
| `world/countries.json`          | Список доступных стран | США, Германия, Бразилия                                     |
| `world/country-archetypes.json` | Архетипы экономик      | rich/poor архетипы с разными параметрами налогов и инфляции |
| `world/commons/skills.json`     | Навыки игрока          | Дерево навыков для развития персонажа                       |

### 🏙️ Country Specifics (Локализация Контента)

_Данные, уникальные для каждой страны (US, Germany, Brazil)._
_Пример для США: `shared/data/world/countries/us/`_

| Файл               | Назначение                           |
| :----------------- | :----------------------------------- |
| `businesses.json`  | Типы бизнесов (cost, roles)          |
| `economy.json`     | Параметры экономики (inflation, gdp) |
| `housing.json`     | Рынок недвижимости                   |
| `jobs.json`        | Вакансии работы                      |
| `shop-categories/` | Товары (food, health, services)      |

---

## 4. ⚙️ Layer 2: Data Ingestion (Загрузчики)

_Модули, преобразующие сырые JSON в типизированные структуры TypeScript._

**Расположение:** `core/lib/data-loaders/`

**Назначение**: Принудительная валидация данных (Runtime Validation) через Zod перед попаданием в стейт.

| Loader Module           | Источник данных                           |
| :---------------------- | :---------------------------------------- |
| `economy-loader.ts`     | `economy.json`                            |
| `jobs-loader.ts`        | `jobs.json`                               |
| `static-data-loader.ts` | **Master Loader** (агрегирует все данные) |

---

## 5. 🧠 Layer 3: Core Logic (Вычислительное Ядро)

_Этот слой реализован как **Headless** (безголовый) — он не содержит кода интерфейса (UI) и ничего не знает о React. Он работает с чистыми данными и может быть протестирован отдельно от визуальной части._

### 🔄 Turn Pipeline (Оркестрация Хода)

**Расположение:** `core/model/logic/`

Система управления ходом построена на паттерне **Pipeline**:

1.  **TurnOrchestrator** (`turn/turn-orchestrator.ts`) - запускает процесс.
2.  **Steps** (`steps/*.step.ts`) - изолированные шаги обработки (Economy -> Market -> Business -> ...).
3.  **TurnState** - мутабельное состояние, передаваемое между шагами.

### 🧮 Calculation Engines (Чистые Функции)

**Расположение:** `core/lib/`

Набор "движков", выполняющих сложные расчеты. Используются внутри шагов Pipeline.

#### 💰 Financial Engine (`core/lib/calculations/`)

| Модуль                        | Назначение                                  |
| :---------------------------- | :------------------------------------------ |
| `inflation/engine.ts`         | Движок инфляции (расчет годового роста цен) |
| `loan/loan-calculator.ts`     | Кредитный калькулятор                       |
| `calculateQuarterlyReport.ts` | Формирование финансового отчета             |

#### 🏢 Business Engine (`core/lib/business/`)

| Модуль                     | Назначение                            |
| :------------------------- | :------------------------------------ |
| `business-financials.ts`   | Расчет прибыли/убытков бизнеса        |
| `business-metrics.ts`      | Расчет KPI (эффективность, репутация) |
| `employee-calculations.ts` | Расчет эффективности сотрудников      |
| `npc-voting.ts`            | Логика голосования партнеров          |

---

## 6. 🎨 Layer 4: Features (UI & Feature Logic)

**Расположение:** `features/`

Реализация UI и специфичной логики представления. Организовано по функциональным фичам.

| Feature            | Описание                                                                     |
| :----------------- | :--------------------------------------------------------------------------- |
| **activities/**    | Игровые активности (Bank, Shop, Work, Education, Family). Основной геймплей. |
| **gameplay/**      | Компоновка игрового экрана (Layouts, Panels).                                |
| **menu/**          | Главное меню и навигация.                                                    |
| **setup/**         | Экраны создания персонажа и выбора страны.                                   |
| **notifications/** | Система уведомлений (Toasts, History).                                       |
| **reports/**       | Модальные окна отчетов (годовой отчет и т.д.).                               |

Каждая фича обычно содержит:

- `components/` - React компоненты.
- `hooks/` - UI логика и связка со стором.
- `lib/` - локальные хелперы.
